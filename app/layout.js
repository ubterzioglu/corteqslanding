export const metadata = {
  title: "Corteqs | AI-Powered Matchmaking for Businesses and Users",
  description:
    "Corteqs waitlist: businesses, organizations, end users and collaborators can join early and get launch benefits.",
};

import "./globals.css";

export default function RootLayout({ children }) {
  return (
    <html lang="tr">
      <body>{children}</body>
    </html>
  );
}
