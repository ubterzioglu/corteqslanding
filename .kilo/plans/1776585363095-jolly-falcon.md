# Plan: Referans Frontend Dosyalarını Ana Projeye Uygula

## Hedef
`referans/src/` altindaki Lovable frontend dosyalarini mevcut projeye aynen uygula. SEO/geo ayarlari ve backend (supabase, edge functions, lib modulleri) korunacak.

---

## KORUNACAK Dosyalar (Dokunulmayacak)

| Dosya/Dizin | Neden |
|-------------|-------|
| `index.html` | SEO: GA4, Clarity, JSON-LD, OG tags, sitemap |
| `src/App.tsx` | Shortlink route'lari (/s, /d) + Admin route |
| `src/pages/Index.tsx` | E2E test render-complete event |
| `src/pages/AdminPage.tsx` | Ozel admin paneli |
| `src/pages/ShortlinkDPage.tsx` | Shortlink redirect |
| `src/pages/ShortlinkSPage.tsx` | Shortlink redirect |
| `src/pages/ExternalRedirectPage.tsx` | Redirect component |
| `src/integrations/*` | Supabase client + types (admin_users, status, notes) |
| `src/lib/submissions.ts` | toSubmissionInsert helper |
| `src/lib/mail.ts` | notifySubmission (Resend API) |
| `src/lib/admin.ts` | userIsAdmin |
| `src/lib/submissions.test.ts` | Unit testleri |
| `supabase/*` | Tum migration'lar + edge functions |
| `public/*` | SEO dosyalari (sitemap, og-image, robots.txt, env-config.js) |
| `tailwind.config.ts` | Mevcut ESM import daha iyi |
| `.env*` | Secret dosyalari |
| `Docker*`, `docker*` | Docker yapilandirmasi |

---

## DOGRUDAN KOPYALANACAK Dosyalar (9 dosya)

Referans layout'nu aynen uygula, backend baglantisi olmayanlar:

| # | Dosya | Degisiklik |
|---|-------|-----------|
| 1 | `src/App.css` | `#root { min-height: 100vh }` → `/* App-level styles */` |
| 2 | `src/components/HeroSection.tsx` | Watercolor gorsel + basit tasarim |
| 3 | `src/components/AboutSection.tsx` | Temiz layout, basit icerik, `1M+` stat |
| 4 | `src/components/AmbassadorSection.tsx` | rounded-xl tasarim, gradient overlay |
| 5 | `src/components/BloggerSection.tsx` | rounded-xl tasarim, gradient overlay |
| 6 | `src/components/CategoriesSection.tsx` | rounded-xl tasarim, baslik degisikligi |
| 7 | `src/components/NavLink.tsx` | Zaten ayni — atlanabilir |
| 8 | `src/assets/hero-watercolor.png` | Zaten kopyalandi |
| 9 | `src/assets/hero-landmarks-watercolor.png` | Zaten kopyalandi |

---

## MERGE EDILECEK Dosyalar (4 dosya)

Referans layout'ni al + mevcut backend entegrasyonunu koru:

### 8. `src/components/FooterSection.tsx`
- **Referans layout**: rounded-xl butonlar, gradient Instagram, Twitter ikonu
- **Mevcut backend**: Gercek WhatsApp URL (`L3FeJVRpPIb75bQGG7M3oN`)
- **Strateji**: Referans tasarimi + mevcut WhatsApp URL

### 9. `src/components/SupportSection.tsx`
- **Referans layout**: 2-tier (Ulke Bazli $1000 + Onursal $10000) + custom amount + Heart ikonu
- **Mevcut backend**: BackerForm import
- **Strateji**: Referans tasarimi aynen uygula (2-tier + custom amount)

### 10. `src/components/BackerForm.tsx`
- **Referans layout**: 2-tier selector + custom amount + emoji butonlar
- **Mevcut backend**: `notifySubmission()` + `toSubmissionInsert()` + `err: unknown` tiplemesi
- **Strateji**: Referans tasarim + mevcut backend helper'larini koru
  - `import { notifySubmission } from "@/lib/mail"`
  - `import { toSubmissionInsert } from "@/lib/submissions"`
  - Submit handler'da `toSubmissionInsert()` + `notifySubmission()` kullan

### 11. `src/components/RegisterInterestForm.tsx`
- **Referans layout**: inline kategoriler, emoji'ler, rounded-lg tasarim
- **Mevcut backend**: `notifySubmission()` + `categoryOptions` + `toSubmissionInsert()` + `SubmissionFormMode` tipi
- **Strateji**: Referans tasarim + mevcut backend helper'larini koru
  - `import { notifySubmission } from "@/lib/mail"`
  - `import { categoryOptions, toSubmissionInsert, type SubmissionFormMode } from "@/lib/submissions"`
  - Submit handler'da `toSubmissionInsert()` + `notifySubmission()` kullan

---

## Adimlar

1. **Dogrudan kopyala**: HeroSection, AboutSection, AmbassadorSection, BloggerSection, CategoriesSection, App.css → referans versiyonu ile degistir
2. **Merge - FooterSection**: Referans tasarim + mevcut WhatsApp URL koru
3. **Merge - SupportSection**: Referans tasarim aynen uygula
4. **Merge - BackerForm**: Referans tasarim + notifySubmission/toSubmissionInsert entegrasyonu
5. **Merge - RegisterInterestForm**: Referans tasarim + notifySubmission/toSubmissionInsert/categoryOptions entegrasyonu
6. **Dogrulama**: `npm run build` + `npm run lint`

---

## Toplam Etki
- **Degisecek**: ~11 dosya (6 kopya + 4 merge + App.css)
- **Korunacak**: ~20+ dosya (backend, SEO, admin, shortlinks, config)
- **Yeni Gorseller**: 2 watercolor PNG (zaten mevcut)
